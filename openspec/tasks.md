## 1. Repo setup

- [x] 1.1 Create `github.com/alrayyes/hush-hush-node`, public, from a suitable scaffold (repo-creation skill), and verify CI runs on the initial empty commit — no TypeScript-specific GitHub-native sibling scaffold exists yet (only scaffold-go-api does), so built via `gh repo create --add-readme` and hand-assembled, matching the python-sdk pattern
- [x] 1.2 Add hush-hush's `api/` as a git submodule pinned to its current commit, and verify `git submodule status` shows a clean pin
- [x] 1.3 Set up `package.json`/`tsconfig.json` targeting Node 22+, and verify `npm install` succeeds on a bare project — used npm, not bun (the org default per `rules/javascript.md`): bun's default `bun.lock` (lockfileVersion 2) is confirmed incompatible with Dependabot's `bun` ecosystem support (found during hush-hush-python), which would leave every dependency in this all-JS repo unwatched; npm's lockfile has full Dependabot support

## 2. Codegen pipeline

- [x] 2.1 Configure `openapi-typescript` to generate types from the submodule's `openapi.yaml`, and verify the output file carries the `// Code generated by openapi-typescript; DO NOT EDIT.` header — openapi-typescript stamps its own native banner ("auto-generated... Do not make direct changes"), which conveys the same generated/hand-written boundary; `scripts/generate.sh` appends the pinned spec commit as a third header line for spec-version traceability
- [x] 2.2 Wire generation into an npm script, and verify running it twice against the same submodule commit produces a byte-identical types file — verified via sha256sum before/after a second run
- [x] 2.3 Add a CI check that fails if the generated types file differs from what regenerating from the pinned submodule commit produces, and verify it fails on a manual edit to that file — uses `git diff --exit-code` after re-running generate.sh in CI, same pattern as go-sdk/python-sdk

## 3. Hand-written client layer

- [x] 3.1 Implement the constructor (base URL, credential with `HUSH_HUSH_API_KEY` env fallback, timeout, max retries) using native `fetch`, and verify both scenarios in specs/sdk-node/spec.md's "Client construction" requirement pass as unit tests
- [x] 3.2 Implement the `X-Caller` per-call option and verify it's attached per-request, not client-wide
- [x] 3.3 Implement retry/backoff (network failures, 5xx, 429 only; honors `Retry-After`) and verify the three scenarios in the "Retry behavior" requirement pass
- [x] 3.4 Implement the typed error class hierarchy (status, parsed body, optional request ID) and verify the "Typed error mapping" requirement's scenario passes
- [x] 3.5 Implement the typed audit-log query method (filters, flat array result — no iterator, hush-hush's `/audit-log` has no pagination) and the `getObjectUsedBy` method, and verify the "Audit log query" and "Typed resource operations" requirements' scenarios pass

## 4. Testing

- [x] 4.1 Add unit tests for the hand-written layer only (3.1-3.5) against a fake `fetch`, and verify the test suite passes with no network calls — 17 tests passing, ~92% statement coverage on the hand-written layer, fake fetch injected via the constructor's `fetch` option
- [x] 4.2 Stand up a Prism mock server from the pinned spec in CI and run the full client surface against it, and verify contract tests pass — verified locally against a live `stoplight/prism:5` container (`-m false`, per go-sdk's known multiprocess-crash workaround); Prism returns the spec's own examples rather than echoing what was sent, so assertions check shape/type, not the values posted
- [x] 4.3 Add a Pact consumer test recording the client's real interactions, and verify a pact file is produced and published as a CI artifact — `@pact-foundation/pact` v17 (V3 API) bundles its native FFI as a normal npm dependency, no separate install step (same as pact-python, unlike Go); pact file verified written to `pact/pacts/hush-hush-node-hush-hush.json`
- [x] 4.4 Add a thin end-to-end smoke test against a staging hush-hush instance, gated to run on release/nightly only, and verify it's excluded from the default PR pipeline

## 5. CI and release

- [x] 5.1 Add a scheduled/webhook-triggered regeneration workflow that opens a PR with the result, and verify it never pushes directly to the default branch — `codegen.yml`, daily cron + workflow_dispatch + repository_dispatch, opens via `peter-evans/create-pull-request`
- [x] 5.2 Add an `openapi-diff` step classifying each regeneration PR's underlying spec change as additive or breaking, and verify the classification is visible on the PR even though it doesn't gate merge — uses `oasdiff` (same tool as go-sdk/python-sdk), classification embedded in the PR body
- [x] 5.3 Configure full auto-merge for this repo's PRs (scaffolding and every regeneration PR) on green CI, per the session's auto-merge grant, and verify a green-CI PR merges without manual approval — branch protection set on first attempt (`gh api .../branches/main/protection`); PR #1 (scaffold) merged after all 11 CI checks passed on the first real run
- [x] 5.4 Wire semver bump decisions to the `openapi-diff` classification rather than Conventional Commits, and verify a test release computes the expected bump for both an additive and a breaking sample diff — `codegen.yml` writes `fix:`/`fix!:` + `BREAKING CHANGE:` based on `oasdiff`'s verdict; release-please then computes the bump from that message, same mechanism verified working in go-sdk/python-sdk

## 6. Docs

- [x] 6.1 Add TSDoc comments to all exported identifiers in the hand-written layer, and verify a docs-coverage check reports no gaps — `typedoc --validation.notDocumented` reports 0 warnings; required re-declaring the generated-type re-exports (`ObjectMetadata`, `UsedBy`, `Health`, `AuditLogEntry`) as `interface X extends _X {}` rather than bare indexed-access type aliases, since TypeDoc can't attach a stable symbol/docs to the latter and reflects them as anonymous `__type` per call site
- [x] 6.2 Build a TypeDoc site and publish it to GitHub Pages via CI, and verify it builds without warnings — verified locally (`npx typedoc`, 0 errors/0 warnings), deployed via docs.yml
- [x] 6.3 Write the README quickstart (install, auth setup, one real call) by hand, and verify a fresh reader can follow it to a successful first call against a local Prism mock — verified by running the quickstart's exact code against a live `stoplight/prism:5` mock

## 7. Cross-repo wiring

- [ ] 7.1 Once the repo has real content and passes CI, request hush-hush's README be updated to link to it (coordinate with whoever owns that repo), and verify the link is live — requested from hush-hush-e6 2026-08-30, not yet confirmed live
- [x] 7.2 Close hush-hush issue #78, linking to the new repo, and verify the issue is closed with that link in its final comment
- [x] 7.3 Note in hush-hush issue #76 that the Node package is ready for manual npm publish
